# @geminixiang/pi-task-protocol

Small, versioned TypeScript contract shared by `pi-supervisor` clients and taskd. It defines task specifications, lifecycle states/events, NDJSON request/reply envelopes, validation, and transition guards.
