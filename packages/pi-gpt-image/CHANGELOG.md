# Changelog

## 0.1.0

- Add the `gpt_image` Pi tool for generating and editing images through Codex OAuth and gpt-image-2.
- Add configurable output persistence, image validation, SSE parsing, and transient-request retries.
